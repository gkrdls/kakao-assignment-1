## 과제 목표
이번 과제를 통해 무엇을 배우고자 했는지 간단하게 작성해요.

- 
- 
-

---

## 과제 위치
- 브랜치명 : `week-01-이학인`
- 주요 파일 : `index.html` / `style.css` / `app.js`

---

## 구현한 기능
기본 미션 중 구현한 항목에 체크해요.

- [x] Todo CRUD 구현하기
- [x] 상태 필터 추가하기
- [x] Todo 일간 뷰 구현하기
- [x] 로컬스토리지 연동하기

---

## 도전 기능
도전 미션 구현을 시도했다면 구현 항목에 체크해요.

- [ ] 기능명

---

## AI 활용 내역


### Todo CRUD 구현하기
- AI 활용 내용: 다음 프롬프트를 사용하여 기본 기능을 개발했습니다. 입력창, 추가 버튼, Todo 목록이 포함된 HTML 레이아웃과 수정 / 완료 / 삭제 버튼이 각 항목에 달린 기본 CRUD 코드를 받았습니다.
```
Todo 앱의 HTML 레이아웃과 기본 CRUD 기능을 만들어줘.

- 텍스트 입력창과 추가 버튼으로 Todo를 생성할 수 있어야 해
- 생성된 Todo는 목록으로 표시되고, 각 항목마다 수정 / 완료 / 삭제 버튼이 있어야 해
- 입력값이 비어있으면 Todo가 생성되지 않고 안내 메시지를 표시해줘
- 완료된 Todo는 텍스트에 취소선이 표시되어야 해
```
- 직접 수정한 부분: body 영역의 background color를 #c0c0c0로 변경했습니다. 그리고 todo-item의 background color를 #d0c4ff로 변경했습니다.

- 수정 이유: todo-pannel의 색이 #ffffff로 설정되어 있어 가시성이 떨어졌기 때문입니다. todo-item의 색도 같은 이유로 변경하였습니다.
### 상태 필터 추가하기
- AI 활용 내용: 다음 프롬프트를 사용하여 상태 필터 기능을 추가하였습니다.
```
기존 코드에 상태별 필터링 기능을 추가해줘.

- 전체 / 진행 중 / 완료 탭을 클릭하면 해당 상태의 Todo만 표시돼야 해
- 현재 선택된 탭은 시각적으로 구분되는 스타일이 적용되어야 해
```
- 변경된 사항: index.html에서 전체, 진행 중, 완료 탭이 추가되었습니다.
style.css에 탭 버튼 스타일이 추가되었습니다.
app.js에는 현재 선택된 필터에 따라 Todo배열을 가져오는 함수가 추가되었으며, 이에따라 렌더링함수도 현재 선택된 필터에 대한 배열로 렌더링하도록 변경되었습니다.

- 직접 수정한 부분: 빈 목록에 대한 안내메시지를 출력하도록 변경하였습니다. html 파일에는 빈 목록에 대한 empty-message 클래스를 추가하고, css 파일에는 empty-message에 대한 스타일을 추가하였으며, 그리고 js 파일에는 목록이 비어있을 때 현재 상태에 맞는 안내 문구를 보여주도록 하는 함수(updateEmptyMessage)를 추가하였습니다.

- 수정 이유: 필터 버튼을 눌렀을 때 해당 상태의 Todo가 없으면 화면이 그냥 비어보여서 사용자가 오류라고 헷갈릴 수 있다고 생각하였습니다. 그래서 상태별로 안내 문구가 나오도록 수정하였습니다.

### Todo 일간 뷰 구현하기
- AI 활용 내용: 다음 프롬프트를 사용하여 Todo 일간 뷰 기능을 추가하였습니다.
```
기존 코드에 일간 뷰 기능을 추가해줘.

- 화면 상단에 오늘 날짜를 표시하고, 이전 / 다음 버튼으로 날짜를 이동할 수 있어야 해
- Todo를 생성할 때 현재 선택된 날짜를 함께 저장해야 해
- 선택된 날짜에 해당하는 Todo만 목록에 표시돼야 해
```
- 변경된 사항: html 파일에는 날짜를 선택할 수 있는 div를 추가하였습니다. 해당 div에는 이전 날짜와 다음 날짜를 선택할 수 있는 버튼 두 개와 현재 선택된 날짜를 표시하는 p가 있습니다.
css 파일에는 추가된 요소에 대한 스타일을 추가로 작성하였으며, js파일에는 날짜를 표기할 수 있는 Date객체와 날짜를 변경하는 moveDate함수, 현재 선택된 날짜를 사용자가 읽기 쉬운 형식으로 표시하는 updateSelecetedDateText함수, Date 객체를 문자열 객체로 포매팅하는 formatDateKey함수를 추가하였으며 emptyMessages도 변경된 사항에 맞추어 바꿨습니다.

- 직접 수정한 부분: html 파일에서 이전 / 다음 문구를 어제 / 내일 로 변경하였다.
- 수정한 이유: 날짜 이동 버튼의 의미를 더 직관적으로 전달하기 위함이다. 사용자가 버턴을 눌렀을 때 날짜가 하루 단위로 이동한다는 점을 더 쉽게 이해할 수 있을 것 같다.

### 로컬스토리지 연동하기
- AI 활용 내용: 다음 프롬프트를 사용하여 로컬스토리지 연동 기능을 추가하였습니다.
```
기존 코드에 로컬스토리지 연동 기능을 추가해줘.

- Todo를 추가 / 수정 / 삭제 / 완료 처리할 때마다 로컬스토리지에 저장되어야 해
- 페이지를 새로고침하면 로컬스토리지에서 데이터를 불러와 목록을 복원해야 해
- 데이터는 JSON.stringify / JSON.parse를 사용해서 저장하고 불러와줘
```
- 변경된 사항: html, css 파일에는 변경 사항이 없고 js 파일에 저장/불러오기 로직이 추가되었습니다.
TODO_STORAGE_KEY 값으로 저장소 키를 설정하고, saveTodosToStorage함수에서 Todo 배열을 문자열로 변환하여 저장하고, 페이지가 다시 열릴 때 loadTodosFromStrage함수에서 배열로 복원하도록 하였습니다. 또한 Todo를 추가, 수정, 완료, 취소, 삭제할 때마다 저장 함수가 실행되도록 하였습니다.

- 직접 수정한 부분:
- 수정한 이유
---

## 구현하면서 고민한 점
구현 과정에서 막혔던 부분이나 고민했던 내용, 해결 방법을 자유롭게 작성해요.

- 고민한 점 : 
- 해결 방법 : 

---

## 과제 회고
과제를 마치고 느낀 점, 아쉬운 점, 다음에 개선하고 싶은 점을 자유롭게 작성해요.

- 잘한 점 : 
- 아쉬운 점 : 
- 다음에 시도해볼 것 :